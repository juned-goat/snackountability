# Copy to .env.local and fill in from your Supabase project's dashboard
# (Project Settings -> API). The anon key is safe to ship in the client --
# that's what it's for -- but never put the service_role key in this app.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
