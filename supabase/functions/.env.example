# Local dev only (used with `pnpm db:functions:serve`). For the deployed
# functions, set these as real secrets instead:
#   supabase secrets set --env-file supabase/functions/.env.local
# Never commit the filled-in version of this file.
OPENROUTER_API_KEY=
USDA_FDC_API_KEY=
